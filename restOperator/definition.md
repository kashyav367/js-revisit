Rest operator = (...rest) allow a function work by bundling them into array
                
                spread vs rest
                Spread = expands an array into separate elements
                rest = bundles separate elements into an array
                